#pragma once

#include <memory>

namespace ofxOrbbecPersee {

  class ImageStream;
  typedef std::shared_ptr<ImageStream> ImageStreamRef ;

  class ImageStream {
    public: // types & consts

    public: // methods
      void draw() {}

    private: // attributes
  };
}
